Este commit contiene los cambios dentro de los siguientes ficheros: 

setting => (PROBLEMAS CON LA CONEXION A LA BD, EL FICHERO ENV NO FUNCIONA CORRECTAMENTE),

models => se modifico models con cambios en los campos rol, username, email, etc

urls => se agregaron tres endpoints (login, registro, refresh-token) mira los endpoints de api/usuarios/. Con el metodo post
siempre devuelven 401 en las peticiones 

views => se crearon los metodos de los endpoints arriba descritos con sus validaciones de serializers, jwt 
y devolviendo una respuesta al frontend en json, adicionalmente se agrego httpOnly a la peticion para gestionar en el front 
los tokens de una manera segura

